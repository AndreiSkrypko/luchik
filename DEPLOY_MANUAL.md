# 🛠️ Ручной деплой на Render.com (если Blueprint не работает)

Если Blueprint не работает или показывает пустую страницу, создайте сервисы вручную:

---

## ШАГ 1: Создание базы данных

1. В панели Render нажмите **"+ Добавить новый"** → **"Постгрес"** (Postgres)
2. Заполните:
   - **Имя**: `luchik-db`
   - **База данных**: `luchik`
   - **Пользователь**: `luchik`
   - **План**: **Бесплатный** (Free)
3. Нажмите **"Создать базу данных"** / **"Create Database"**
4. Дождитесь создания (~2-3 минуты)

---

## ШАГ 2: Создание Backend (Django)

1. Нажмите **"+ Добавить новый"** → **"Веб-сервис"** (Web Service)
2. **Подключите репозиторий**:
   - Выберите **GitHub**
   - Выберите репозиторий `luchik`
   - Нажмите **"Соединять"** / **"Connect"**
3. **Настройки сервиса**:
   - **Имя**: `luchik-backend`
   - **Окружение**: Выберите **"Docker"**
   - **Dockerfile Path**: `./backend/Dockerfile`
   - **Docker Context**: `./backend`
   - **План**: **Бесплатный** (Free)
4. **Переменные окружения** (раздел "Environment"):
   - Нажмите **"Добавить переменную окружения"**
   - Добавьте:
     - `DJANGO_SECRET_KEY` = (сгенерируйте на https://djecrety.ir/)
     - `DJANGO_DEBUG` = `False`
     - `ALLOWED_HOSTS` = `luchik-backend.onrender.com`
5. **Подключение к базе данных**:
   - В разделе **"Database"** выберите `luchik-db`
   - Render автоматически добавит переменные `POSTGRES_*`
6. Нажмите **"Создать веб-сервис"** / **"Create Web Service"**

---

## ШАГ 3: Создание Frontend (Next.js)

1. Нажмите **"+ Добавить новый"** → **"Веб-сервис"** (Web Service)
2. **Подключите репозиторий**:
   - Выберите тот же репозиторий `luchik`
3. **Настройки сервиса**:
   - **Имя**: `luchik-frontend`
   - **Окружение**: Выберите **"Docker"**
   - **Dockerfile Path**: `./frontend/Dockerfile`
   - **Docker Context**: `./frontend`
   - **План**: **Бесплатный** (Free)
4. **Переменные окружения**:
   - `NEXT_PUBLIC_API_BASE_URL` = `https://luchik-backend.onrender.com`
     (замените на реальный URL вашего backend после его создания)
5. Нажмите **"Создать веб-сервис"** / **"Create Web Service"**

---

## ШАГ 4: Настройка CORS

После того как оба сервиса создадутся:

1. Откройте **luchik-backend**
2. Перейдите в **"Переменные окружения"** / **"Environment"**
3. Добавьте:
   - `CORS_ALLOWED_ORIGINS` = `https://luchik-frontend.onrender.com`
     (замените на реальный URL вашего frontend)
4. Backend автоматически перезапустится

---

## ШАГ 5: Применение миграций

1. Откройте **luchik-backend**
2. Перейдите в **"Оболочка"** / **"Shell"**
3. Выполните:
   ```bash
   python manage.py migrate
   ```

---

## ✅ Готово!

Ваши сервисы будут доступны:
- Backend: `https://luchik-backend.onrender.com`
- Frontend: `https://luchik-frontend.onrender.com`

---

## 🔍 Если что-то не работает:

1. **Проверьте логи**: Раздел **"Логи"** / **"Logs"** в каждом сервисе
2. **Проверьте переменные окружения**: Убедитесь, что все переменные добавлены
3. **Проверьте статус**: Сервисы должны быть в статусе **"Активен"** / **"Live"**

