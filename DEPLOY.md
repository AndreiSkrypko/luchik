# 🚀 Пошаговая инструкция деплоя на Render.com

## Подготовка проекта (уже сделано ✅)

1. ✅ Создан `render.yaml` - конфигурация для автоматического деплоя
2. ✅ Созданы `.dockerignore` файлы
3. ✅ Dockerfile'ы готовы

---

## ШАГ 1: Регистрация на Render.com

1. Откройте https://render.com
2. Нажмите **"Начните бесплатно"** (Start for free)
3. Войдите через **GitHub** (рекомендуется) или создайте аккаунт
4. Подтвердите email

---

## ШАГ 2: Подключение GitHub репозитория

1. В панели Render нажмите **"New +"** → **"Blueprint"**
2. Выберите **"Public Git repository"**
3. Подключите ваш GitHub аккаунт (если еще не подключен)
4. Выберите репозиторий `luchik`
5. Render автоматически обнаружит файл `render.yaml`

---

## ШАГ 3: Настройка деплоя

1. Render покажет превью конфигурации из `render.yaml`
2. Проверьте, что видны 3 сервиса:
   - **luchik-backend** (Django API)
   - **luchik-frontend** (Next.js)
   - **luchik-db** (PostgreSQL)
3. Нажмите **"Apply"**

---

## ШАГ 4: Настройка переменных окружения

### Для Backend (luchik-backend):

1. После создания сервиса, откройте **luchik-backend**
2. Перейдите в **"Environment"**
3. Добавьте переменную:
   - **Key**: `DJANGO_SECRET_KEY`
   - **Value**: Сгенерируйте секретный ключ:
     ```bash
     python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
     ```
   - Или используйте онлайн генератор: https://djecrety.ir/

4. Проверьте, что автоматически добавлены переменные от базы данных (Render добавит их сам)

5. **ВАЖНО**: После деплоя frontend, добавьте переменную:
   - **Key**: `CORS_ALLOWED_ORIGINS`
   - **Value**: URL вашего frontend (например: `https://luchik-frontend.onrender.com`)
   - Это нужно для того, чтобы frontend мог делать запросы к backend

### Для Frontend (luchik-frontend):

1. Откройте **luchik-frontend**
2. Перейдите в **"Environment"**
3. Проверьте, что `NEXT_PUBLIC_API_BASE_URL` указывает на backend URL
   - Формат: `https://luchik-backend.onrender.com`
   - Render должен добавить это автоматически

---

## ШАГ 5: Запуск деплоя

1. Render начнет деплой автоматически
2. Сначала создастся база данных **luchik-db** (~2-3 минуты)
3. Затем соберется и запустится **backend** (~5-7 минут)
4. Затем соберется и запустится **frontend** (~5-7 минут)

**Итого: ~12-17 минут на первый деплой**

---

## ШАГ 6: Применение миграций

После того как backend запустится:

1. Откройте **luchik-backend** в панели Render
2. Перейдите в **"Shell"** (или используйте **"Manual Deploy"** → **"Run Command"**)
3. Выполните:
   ```bash
   python manage.py migrate
   ```
4. (Опционально) Создайте суперпользователя:
   ```bash
   python manage.py createsuperuser
   ```

---

## ШАГ 7: Проверка работы

1. **Backend**: Откройте `https://luchik-backend.onrender.com/api/home/`
   - Должен вернуться JSON с данными

2. **Frontend**: Откройте `https://luchik-frontend.onrender.com`
   - Должна открыться главная страница

---

## 🔄 Автоматический деплой

После первого деплоя:
- При каждом **push в main/master** ветку Render автоматически пересоберет и задеплоит изменения
- Это займет ~5-10 минут

---

## ⚙️ Дополнительные настройки

### Изменить домены

1. В настройках каждого сервиса → **"Settings"** → **"Custom Domain"**
2. Добавьте свой домен (если есть)

### Мониторинг

- Render показывает логи в реальном времени
- Доступны метрики CPU/RAM
- Настройте алерты в **"Alerts"**

### Масштабирование

- На бесплатном тарифе: 1 инстанс каждого сервиса
- Для продакшена: перейдите на платный план в **"Settings"** → **"Plan"**

---

## 🐛 Решение проблем

### Backend не запускается
- Проверьте логи в Render Dashboard
- Убедитесь, что `DJANGO_SECRET_KEY` установлен
- Проверьте подключение к БД (переменные `POSTGRES_*`)

### Frontend не подключается к Backend
- Проверьте `NEXT_PUBLIC_API_BASE_URL` в переменных окружения
- Убедитесь, что backend URL правильный (с `https://`)

### База данных не создается
- Подождите 2-3 минуты (Render создает БД асинхронно)
- Проверьте логи в разделе **"Events"**

---

## 📝 Важные замечания

1. **Бесплатный тариф**: Сервисы "засыпают" после 15 минут неактивности. Первый запрос может занять ~30 секунд на "пробуждение"

2. **Лимиты**: 
   - 750 часов/месяц бесплатно
   - Достаточно для тестирования и небольших проектов

3. **Переменные окружения**: Никогда не коммитьте `.env` файлы в Git. Используйте только UI Render для секретов

---

## ✅ Готово!

После выполнения всех шагов ваш сайт будет доступен по адресам:
- Frontend: `https://luchik-frontend.onrender.com`
- Backend API: `https://luchik-backend.onrender.com`

Удачи! 🎉

