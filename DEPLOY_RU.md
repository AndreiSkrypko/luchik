# 🚀 Пошаговая инструкция деплоя на Render.com (Русский интерфейс)

## Подготовка проекта (уже сделано ✅)

1. ✅ Создан `render.yaml` - конфигурация для автоматического деплоя
2. ✅ Созданы `.dockerignore` файлы
3. ✅ Dockerfile'ы готовы

---

## ШАГ 1: Регистрация на Render.com

1. Откройте https://render.com
2. Нажмите кнопку **"Начните бесплатно"** (большая черная кнопка справа)
3. Войдите через **GitHub** (рекомендуется) - нажмите **"Войти через GitHub"**
4. Подтвердите доступ Render к вашему GitHub аккаунту
5. Подтвердите email (если потребуется)

---

## ШАГ 2: Создание Blueprint (Схема развертывания)

1. После входа вы попадете в **"Панель инструментов"** (Dashboard)
2. В правом верхнем углу нажмите кнопку **"Создать"** или **"New +"** (зеленая кнопка)
3. В выпадающем меню выберите:
   - **"Blueprint"** или **"Схема развертывания"**
4. Вы увидите страницу "Создать Blueprint"

---

## ШАГ 3: Подключение GitHub репозитория

1. На странице создания Blueprint выберите:
   - **"Public Git repository"** или **"Публичный репозиторий Git"**
2. Если GitHub еще не подключен:
   - Нажмите **"Подключить GitHub"** или **"Connect GitHub"**
   - Войдите в GitHub и разрешите доступ Render
3. После подключения GitHub:
   - В списке репозиториев найдите и выберите **`luchik`**
   - Или введите название репозитория: `AndreiSkrypko/luchik`
4. Render автоматически обнаружит файл `render.yaml` в корне проекта

---

## ШАГ 4: Просмотр и подтверждение конфигурации

1. Render покажет превью конфигурации из `render.yaml`
2. Вы увидите список из 3 сервисов:
   - **luchik-backend** (Django API) - тип: Web Service
   - **luchik-frontend** (Next.js) - тип: Web Service  
   - **luchik-db** (PostgreSQL) - тип: Database
3. Проверьте, что все сервисы отображаются корректно
4. Внизу страницы нажмите кнопку **"Применить"** или **"Apply"**

---

## ШАГ 5: Ожидание создания сервисов

Render начнет создавать сервисы автоматически:

1. **Сначала создастся база данных** `luchik-db` (~2-3 минуты)
   - Статус: "Создание..." / "Creating..."
   
2. **Затем соберется и запустится backend** `luchik-backend` (~5-7 минут)
   - Статус: "Сборка..." / "Building..." → "Развертывание..." / "Deploying..."
   
3. **Затем соберется и запустится frontend** `luchik-frontend` (~5-7 минут)
   - Статус: "Сборка..." / "Building..." → "Развертывание..." / "Deploying..."

**Итого: ~12-17 минут на первый деплой**

Вы можете наблюдать за процессом в реальном времени в разделе **"События"** или **"Events"** каждого сервиса.

---

## ШАГ 6: Настройка переменных окружения для Backend

После того как backend создастся (но может еще собираться):

1. В списке сервисов нажмите на **luchik-backend**
2. В боковом меню слева перейдите в раздел **"Переменные окружения"** или **"Environment"**
3. Нажмите кнопку **"Добавить переменную окружения"** или **"Add Environment Variable"**
4. Добавьте переменную:
   - **Ключ (Key)**: `DJANGO_SECRET_KEY`
   - **Значение (Value)**: Сгенерируйте секретный ключ одним из способов:
     - Онлайн генератор: https://djecrety.ir/ (скопируйте сгенерированный ключ)
     - Или в терминале локально:
       ```bash
       python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
       ```
5. Нажмите **"Сохранить изменения"** или **"Save Changes"**

**Примечание**: Переменные для подключения к базе данных (POSTGRES_*) Render добавит автоматически, их не нужно добавлять вручную.

---

## ШАГ 7: Настройка переменных окружения для Frontend

После того как frontend создастся:

1. В списке сервисов нажмите на **luchik-frontend**
2. В боковом меню слева перейдите в раздел **"Переменные окружения"** или **"Environment"**
3. Проверьте, что переменная `NEXT_PUBLIC_API_BASE_URL` уже добавлена автоматически
   - Она должна указывать на URL backend: `https://luchik-backend.onrender.com`
4. Если переменной нет, добавьте ее вручную:
   - **Ключ**: `NEXT_PUBLIC_API_BASE_URL`
   - **Значение**: `https://luchik-backend.onrender.com` (замените на ваш реальный URL backend)

---

## ШАГ 8: Настройка CORS для Backend

**ВАЖНО**: После того как frontend полностью задеплоится и получит свой URL:

1. Откройте **luchik-backend**
2. Перейдите в **"Переменные окружения"** / **"Environment"**
3. Добавьте переменную:
   - **Ключ**: `CORS_ALLOWED_ORIGINS`
   - **Значение**: URL вашего frontend (например: `https://luchik-frontend.onrender.com`)
   - Это нужно для того, чтобы frontend мог делать запросы к backend
4. После добавления переменной backend автоматически перезапустится

---

## ШАГ 9: Применение миграций базы данных

После того как backend полностью запустится (статус "Активен" / "Live"):

1. Откройте **luchik-backend**
2. В боковом меню слева перейдите в раздел **"Оболочка"** или **"Shell"**
   - Альтернатива: **"Ручное развертывание"** → **"Выполнить команду"** / **"Manual Deploy"** → **"Run Command"**
3. В открывшейся консоли выполните команду:
   ```bash
   python manage.py migrate
   ```
4. Дождитесь завершения миграций (вы увидите сообщения "OK" для каждой миграции)

**Опционально**: Создайте суперпользователя для доступа к админ-панели Django:
```bash
python manage.py createsuperuser
```
(Введите email, username и password по запросу)

---

## ШАГ 10: Проверка работы

1. **Backend API**: Откройте в браузере URL backend (например: `https://luchik-backend.onrender.com/api/home/`)
   - Должен вернуться JSON с данными
   - Если видите JSON - backend работает! ✅

2. **Frontend**: Откройте в браузере URL frontend (например: `https://luchik-frontend.onrender.com`)
   - Должна открыться главная страница сайта
   - Если страница загружается - frontend работает! ✅

---

## 🔄 Автоматический деплой

После первого деплоя:
- При каждом **push в main/master** ветку GitHub, Render автоматически:
  1. Обнаружит изменения
  2. Пересоберет Docker образы
  3. Задеплоит обновления
- Это займет ~5-10 минут на каждый деплой
- Вы можете наблюдать за процессом в разделе **"События"** / **"Events"**

---

## ⚙️ Дополнительные настройки

### Изменить домены

1. Откройте любой сервис (backend или frontend)
2. Перейдите в **"Настройки"** или **"Settings"** (в боковом меню)
3. Найдите раздел **"Пользовательский домен"** или **"Custom Domain"**
4. Нажмите **"Добавить домен"** / **"Add Domain"**
5. Введите ваш домен (например: `api.ваш-сайт.ru` для backend, `ваш-сайт.ru` для frontend)
6. Render предоставит инструкции по настройке DNS

### Мониторинг и логи

- **Логи в реальном времени**: Раздел **"Логи"** / **"Logs"** в каждом сервисе
- **Метрики производительности**: Раздел **"Метрики"** / **"Metrics"** (CPU, RAM, запросы)
- **История развертываний**: Раздел **"События"** / **"Events"** показывает все деплои

### Масштабирование

- **Бесплатный тариф**: 1 инстанс каждого сервиса
- **Платный тариф**: 
  - Перейдите в **"Настройки"** → **"План"** / **"Settings"** → **"Plan"**
  - Выберите нужный план (Starter, Professional, etc.)
  - Можно настроить автомасштабирование

---

## 🐛 Решение проблем

### Backend не запускается

1. Проверьте логи в разделе **"Логи"** / **"Logs"** сервиса backend
2. Убедитесь, что `DJANGO_SECRET_KEY` установлен в переменных окружения
3. Проверьте, что все переменные `POSTGRES_*` добавлены автоматически
4. Убедитесь, что база данных `luchik-db` создана и активна

### Frontend не подключается к Backend

1. Проверьте переменную `NEXT_PUBLIC_API_BASE_URL` в frontend
   - Должна быть: `https://luchik-backend.onrender.com` (с `https://`)
2. Проверьте переменную `CORS_ALLOWED_ORIGINS` в backend
   - Должна быть: `https://luchik-frontend.onrender.com` (URL вашего frontend)
3. Проверьте логи backend - там могут быть ошибки CORS

### База данных не создается

1. Подождите 2-3 минуты (Render создает БД асинхронно)
2. Проверьте раздел **"События"** / **"Events"** базы данных
3. Убедитесь, что выбран бесплатный план (если используете бесплатный тариф)

### Сервисы "засыпают"

- На бесплатном тарифе сервисы автоматически "засыпают" после 15 минут неактивности
- Первый запрос после "сна" может занять ~30 секунд на "пробуждение"
- Это нормальное поведение для бесплатного тарифа
- Для постоянной работы перейдите на платный тариф

---

## 📝 Важные замечания

1. **Бесплатный тариф**: 
   - 750 часов/месяц бесплатно
   - Сервисы "засыпают" после 15 минут неактивности
   - Достаточно для тестирования и небольших проектов

2. **Переменные окружения**: 
   - Никогда не коммитьте `.env` файлы в Git
   - Используйте только UI Render для секретов
   - `DJANGO_SECRET_KEY` должен быть уникальным и секретным

3. **Безопасность**:
   - На продакшене установите `DJANGO_DEBUG=False`
   - Используйте сильные пароли для базы данных
   - Настройте `ALLOWED_HOSTS` правильно

---

## ✅ Готово!

После выполнения всех шагов ваш сайт будет доступен по адресам:
- **Frontend**: `https://luchik-frontend.onrender.com`
- **Backend API**: `https://luchik-backend.onrender.com`

**Поздравляем! Ваш проект задеплоен! 🎉**

---

## 📞 Нужна помощь?

- Документация Render: https://render.com/docs (есть русский перевод)
- Поддержка: через раздел **"Поддержка"** / **"Support"** в панели Render

